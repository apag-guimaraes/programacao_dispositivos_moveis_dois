= Broadcast Receiver

Segundo a Google Developers (2025), "Os apps para Android podem enviar ou receber mensagens de transmissão do sistema Android e de outros apps Android". 
Para entender com mais facilidade os broadcasts, relembre os tratamentos de eventos presentes na linguagem Java. Evento é algo que acontece no sistema:
pode ser sua reinicialização; o aparelho habilitado em modo avião; a bateria sendo carregada, a desabilitação da Wi-fi e assim por diante.
Resumindo: algo acontece, uma classe transmite uma mensagem e outra ficará responsável em tratá-la.

Ainda, de acordo com a  Google Developers (2025), "Os apps podem se registrar para receber transmissões específicas. Quando uma transmissão é enviada,
o sistema a direciona automaticamente para apps que se inscreveram para receber esse tipo específico de transmissão."

Para ocorrer a transmissão da mensagem, é necessário vinculá-la ao um objeto Intent. Através do método setAction é definido o evento que aconteceu. 
Por meio do método putExtra é possível passar mais dados na transmissão da mensagem.

- Referência do texto entre aspas: GOOGLE DEVELOPERS. Visão geral de transmissões.Disponível em: 
https://developer.android.com/guide/components/broadcasts#java. Acesso em: 25 de ago. 2025. 

Broadcast Receiver

image::https://img.youtube.com/vi/XXVsL3njoCQ/0.jpg[width=640, height=480, link="https://www.youtube.com/watch?v=XXVsL3njoCQ"]

Existem duas maneiras de definir um BroadcastReceiver:

- No arquivo AndroidManifest.xml (muitos broadcasts não funcionam mais dessa forma nas APIs superiores à 26);

- Pelo contexto, cuja definição é feita por linha de código dentro de uma classe específica.

Para saber mais sobre as ações de transmissões existentes no Android, acesse na sua máquina a pasta do SDK. Por exemplo, na minha máquina o acesso ao 
arquivo foi: Sdk/platforms/android-31/data/broadcast_actions.txt. 

Houve muitas mudanças na API 33 em relação aos broadcast. Por exemplo, nas mensagens emitidas pelo sistema operacional como: Airplane Mode, CONNECTIVITY_CHANGE, HEADSET_PLUG entre outras devem ser declaradas programaticamente (via registerReceiver()); ao usar um PendingIntent deve ser definido a mutabilidade, através de FLAG_IMMUTABLE ou FLAG_MUTABLE.

Exemplo 01 (App que gera e envia números aleatórios):

- activity_main.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gerar e enviar número"
        android:id="@+id/buttonGerar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>

- MainActivity.java
[source,java]
import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Toast;
import androidx.appcompat.app.AppCompatActivity;
import com.example.seuprojeto.databinding.ActivityMainBinding; // Ajuste para o pacote do seu app
import java.util.Random;
public class MainActivity extends AppCompatActivity {
    private static final String ACTION_NUMERO = "com.example.appgeradorreceptor.ACTION_NUMERO";
    private ActivityMainBinding binding;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        binding = ActivityMainBinding.inflate(layoutInflater);
        setContentView(binding.getRoot());
        binding.buttonGerar.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                int numero = new Random().nextInt(100) + 1;
                Intent intent = new Intent(ACTION_NUMERO);
                intent.setPackage(getPackageName());
                intent.putExtra("numero", numero);
                Toast.makeText(MainActivity.this, String.valueOf(numero), Toast.LENGTH_LONG).show();
                sendBroadcast(intent);
            }
        });
    }
}

- activity_segunda.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gerar e enviar número"
        android:id="@+id/textViewNumero"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>

- SegundaActivity.java
[source,java]
import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import com.example.seuprojeto.databinding.ActivitySegundaBinding; // Ajuste para o pacote do seu app
public class SegundaActivity extends AppCompatActivity {
    private ActivitySegundaBinding binding;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        binding = ActivitySegundaBinding.inflate(layoutInflater);
        setContentView(binding.getRoot());
        int num = getIntent().getIntExtra("numero", -1);
        binding.textViewNumero.setText(String.valueOf(num));
    }
}

- NumeroReceiver.java
[source,java]
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.util.Log;
public class NumeroReceiver extends BroadcastReceiver {
    private static final String ACTION_NUMERO = "com.example.appgeradorreceptor.ACTION_NUMERO";
    @Override
    public void onReceive(Context context, Intent intent) {
        if (intent != null && ACTION_NUMERO.equals(intent.getAction())) {
            int numero = intent.getIntExtra("numero", -1);
            Log.d("RECEPTOR", "número recebido: " + numero);
            Intent activityIntent = new Intent(context, SegundaActivity.class);
            activityIntent.putExtra("numero", numero);
            activityIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
            context.startActivity(activityIntent);
        }
    }
}

- No arquivo AndroidManifest.xml acrescente:
    <receiver
        android:name=".NumeroReceiver"
        android:exported="true">
        <intent-filter>
            <action android:name="com.example.appgeradorreceptor.ACTION_NUMERO" />
        </intent-filter>
    </receiver>

- MainActivity.kt
[source,kotlin]
import android.content.Intent
import android.os.Bundle
import android.widget.Toast
import androidx.appcompat.app.AppCompatActivity
import com.example.seuprojeto.databinding.ActivityMainBinding // Ajuste para o pacote do seu app
import java.util.Random
class MainActivity : AppCompatActivity() {
    private lateinit var binding: ActivityMainBinding
    companion object {
        private const val ACTION_NUMERO = "com.example.appgeradorreceptor.ACTION_NUMERO"
    }
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivityMainBinding.inflate(layoutInflater)
        setContentView(binding.root)
        binding.buttonGerar.setOnClickListener {
            val numero = Random().nextInt(100) + 1
            val intent = Intent(ACTION_NUMERO).apply {
                `package` = packageName 
                putExtra("numero", numero)
            }
            Toast.makeText(this, numero.toString(), Toast.LENGTH_LONG).show()
            sendBroadcast(intent)
        }
    }
}

- NumeroReceiver.kt
[source,kotlin]
import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent
import android.util.Log
class NumeroReceiver : BroadcastReceiver() {
    companion object {
        private const val ACTION_NUMERO = "com.example.appgeradorreceptor.ACTION_NUMERO"
    }
    override fun onReceive(context: Context?, intent: Intent?) {
        // O operador '?.' (Safe Call) e o escopo 'let' garantem que o código só rode se os dados forem válidos (não nulos)
        if (intent != null && ACTION_NUMERO == intent.action) {
            val numero = intent.getIntExtra("numero", -1)
            Log.d("RECEPTOR", "número recebido: $numero") // Interpolação de String usando '$'
            context?.let { ctx ->
                val activityIntent = Intent(ctx, SegundaActivity::class.java).apply {
                    putExtra("numero", numero)
                    addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
                }
                ctx.startActivity(activityIntent)
            }
        }
    }
}

- SegundaActivity.kt
[source,kotlin]
import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.seuprojeto.databinding.ActivitySegundaBinding // Ajuste para o pacote do seu app
class SegundaActivity : AppCompatActivity() {
    private lateinit var binding: ActivitySegundaBinding
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivitySegundaBinding.inflate(layoutInflater)
        setContentView(binding.root)
        // Obtém o valor passado por parâmetro com o valor default nativo
        val num = intent.getIntExtra("numero", -1)
        // Atribui o valor diretamente como String no TextView usando template
        binding.textViewNumero.text = num.toString()
    }
}

Exemplo 02 (App que recebe o número gerado, neste exemplo o receiver recebe o número e grava no SharedPreferences. A activity não inicia quando recebe o broadcast!!!):

- APPGERADOR

- activity_main.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gerar e enviar número"
        android:id="@+id/buttonGerar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>

- MainActivity.java
[source,java]
import android.content.Intent;
import android.os.Bundle;
import android.widget.Button;
import android.widget.Toast;
import androidx.appcompat.app.AppCompatActivity;
import java.util.Random;
public class MainActivity extends AppCompatActivity {
    private static final String ACTION_NUMERO = "com.example.ACTION_NUMERO";
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        Button btnGerar = findViewById(R.id.buttonGerar);
        btnGerar.setOnClickListener(v -> {
            // Gera um valor inteiro de 1 a 100
            int numero = new Random().nextInt(100) + 1;
            Intent intent = new Intent(ACTION_NUMERO);
            // Restringe o envio do broadcast para a ID do pacote do app receptor
            intent.setPackage("com.example.receptornumerobroadcastshared");
            intent.putExtra("numero", numero);
            Toast.makeText(this, String.valueOf(numero), Toast.LENGTH_LONG).show();
            sendBroadcast(intent);
        });
    }
}

- MainActivity.kt
[source,kotlin]
import android.content.Intent
import android.os.Bundle
import android.widget.Toast
import androidx.appcompat.app.AppCompatActivity
import com.example.appgerador.databinding.ActivityMainBinding
import java.util.Random
class MainActivity : AppCompatActivity() {
    private lateinit var binding: ActivityMainBinding
    companion object {
        private const val ACTION_NUMERO = "com.example.ACTION_NUMERO"
    }
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivityMainBinding.inflate(layoutInflater)
        setContentView(binding.root)
        binding.buttonGerar.setOnClickListener {
            val numero = Random().nextInt(100) + 1
            // Configuração expressiva usando o bloco 'apply'
            val intent = Intent(ACTION_NUMERO).apply {
                `package` = "com.example.receptornumerobroadcastshared"
                putExtra("numero", numero)
            }
            Toast.makeText(this, numero.toString(), Toast.LENGTH_LONG).show()
            sendBroadcast(intent)
        }
    }
}

- APPRECEPTOR

- AndroidManifest.xml
[source,xml]
  <receiver
            android:name=".NumeroReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.ACTION_NUMERO"/>
            </intent-filter>
        </receiver>

- activity_main.xml
[source,xml]
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center">
    <TextView
        android:id="@+id/textViewNumero"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Aguardando número..."
        android:textSize="20sp"/>
</LinearLayout>

- MainActivity.java
[source,java]
import android.content.Context;
import android.content.SharedPreferences;
import android.os.Bundle;
import android.widget.TextView;
import androidx.appcompat.app.AppCompatActivity;
public class MainActivity extends AppCompatActivity {
    private TextView textViewNumero;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        textViewNumero = findViewById(R.id.textViewNumero);
    }
    @Override
    protected void onResume() {
        super.onResume();
        // Atualiza a tela buscando o último estado salvo em disco
        atualizarInterface();
    }
    private void atualizarInterface() {
        SharedPreferences prefs = getSharedPreferences("dados", Context.MODE_PRIVATE);
        int numero = prefs.getInt("numero", -1);
        if (numero != -1) {
            textViewNumero.setText(String.valueOf(numero));
        } else {
            textViewNumero.setText("Nenhum número recebido ainda");
        }
    }
}

- NumeroReceiver.java
[source,java]
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.content.SharedPreferences;
public class NumeroReceiver extends BroadcastReceiver {
    private static final String ACTION_NUMERO = "com.example.ACTION_NUMERO";
    @Override
    public void onReceive(Context context, Intent intent) {
        if (intent != null && ACTION_NUMERO.equals(intent.getAction())) {
            int numero = intent.getIntExtra("numero", -1);
            // Obtém a instância de SharedPreferences a partir do contexto do receptor
            SharedPreferences prefs = context.getSharedPreferences("dados", Context.MODE_PRIVATE);
            // Grava o valor recebido no arquivo XML local
            prefs.edit().putInt("numero", numero).apply();
        }
    }
}

- NumeroReceiver.kt
[source,kotlin]
import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent
import androidx.core.content.edit
class NumeroReceiver : BroadcastReceiver() {
    companion object {
        private const val ACTION_NUMERO = "com.example.ACTION_NUMERO"
    }
    override fun onReceive(context: Context?, intent: Intent?) {
        if (intent != null && ACTION_NUMERO == intent.action) {
            val numero = intent.getIntExtra("numero", -1)
            context?.let { ctx ->
                val prefs = ctx.getSharedPreferences("dados", Context.MODE_PRIVATE)
                // Sintaxe idiomática do Kotlin (Jetpack KTX) para edição de Preferences
                prefs.edit {
                    putInt("numero", numero)
                }
            }
        }
    }
}

- MainActivity.kt
[source,kotlin]
import android.content.Context
import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.receptornumerobroadcastshared.databinding.ActivityMainBinding
class MainActivity : AppCompatActivity() {
    private lateinit var binding: ActivityMainBinding
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivityMainBinding.inflate(layoutInflater)
        setContentView(binding.root)
    }
    override fun onResume() {
        super.onResume()
        atualizarInterface()
    }
    private fun actualizarInterface() {
        val prefs = getSharedPreferences("dados", Context.MODE_PRIVATE)
        val numero = prefs.getInt("numero", -1)
        binding.textViewNumero.text = if (numero != -1) {
            numero.toString()
        } else {
            "Nenhum número recebido ainda"
        }
    }
}

Exemplo 03 (Broadcast Modo Avião):

- activity_main.xml
[source,xml]
<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <TextView
        android:id="@+id/textViewStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello world!!"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>

- MyBroadcast.java
[source,java]
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.widget.Toast;
import androidx.fragment.app.FragmentActivity;
public class MyBroadcast extends BroadcastReceiver {
    private final FragmentActivity activity;
    // Construtor customizado para receber a Activity (funciona apenas em registros dinâmicos)
    public MyBroadcast(FragmentActivity activity) {
        this.activity = activity;
    }
    @Override
    public void onReceive(Context context, Intent intent) {
        if (intent != null && Intent.ACTION_AIRPLANE_MODE_CHANGED.equals(intent.getAction())) {
            // "state" é o extra padrão do sistema que traz true (ativado) ou false (desativado)
            boolean status = intent.getBooleanExtra("state", false);
            String msg = status ? "Modo avião ativado!" : "Modo avião desativado!";
            Toast.makeText(context, msg, Toast.LENGTH_SHORT).show();
            // Exibe o diálogo na tela utilizando a Activity passada por parâmetro
            AlertDialogModoAviao dialog = new AlertDialogModoAviao(msg);
            dialog.show(activity.getSupportFragmentManager(), "AirplaneModeDialog");
        }
    }
}
- AlertDialogModoAviao.java
[source,java]
import android.app.Dialog;
import android.os.Bundle;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.appcompat.app.AlertDialog;
import androidx.fragment.app.DialogFragment;

public class AlertDialogModoAviao extends DialogFragment {
    private final String mensagem;

    public AlertDialogModoAviao(String mensagem) {
        this.mensagem = mensagem;
    }

    @NonNull
    @Override
    public Dialog onCreateDialog(@Nullable Bundle savedInstanceState) {
        return new AlertDialog.Builder(requireActivity())
                .setTitle("Modo Avião")
                .setMessage(mensagem)
                .setPositiveButton("OK", null)
                .create();
    }
}

- MainActivity.java
[source,java]
import androidx.appcompat.app.AppCompatActivity;
import android.content.Intent;
import android.content.IntentFilter;
import android.os.Bundle;
import com.example.seuprojeto.databinding.ActivityMainBinding; // Ajuste para o pacote do seu app
public class MainActivity extends AppCompatActivity {
    private ActivityMainBinding binding;
    private MyBroadcast myBroadcast;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState)
        // Inicializa o View Binding
        binding = ActivityMainBinding.inflate(layoutInflater);
        setContentView(binding.root);
        // Altera o texto usando o binding diretamente
        binding.textViewStatus.setText("Monitorando o Modo Avião...");
        // Instancia o receiver passando 'this' (que é uma AppCompatActivity, filha de FragmentActivity)
        myBroadcast = new MyBroadcast(this);
    }
    @Override
    protected void onStart() {
        super.onStart();
        // Registro dinâmico obrigatório para interceptar mudanças no modo avião a partir da API 26
        IntentFilter filter = new IntentFilter(Intent.ACTION_AIRPLANE_MODE_CHANGED);
        registerReceiver(myBroadcast, filter);
    }
    @Override
    protected void onStop() {
        super.onStop();
        // OBRIGATÓRIO: Desregistra o receiver para evitar vazamento de memória (Memory Leak)
        unregisterReceiver(myBroadcast);
    }
}

- MyBroadcast.kt
[source,kotlin]
import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent
import android.widget.Toast
import androidx.fragment.app.FragmentActivity
class MyBroadcast(private val activity: FragmentActivity) : BroadcastReceiver() {
    override fun onReceive(context: Context?, intent: Intent?) {
        if (intent != null && Intent.ACTION_AIRPLANE_MODE_CHANGED == intent.action) {
            // Obtém o estado booleano do modo avião
            val status = intent.getBooleanExtra("state", false)
            // Atribuição condicional idiomática do Kotlin
            val msg = if (status) "Modo avião ativado!" else "Modo avião desativado!"
            Toast.makeText(context, msg, Toast.LENGTH_SHORT).show()
            // Instancia e exibe o diálogo fragmentado
            val dialog = AlertDialogModoAviao(msg)
            dialog.show(activity.supportFragmentManager, "AirplaneModeDialog")
        }
    }
}

-  AlertDialogModoAviao.kt
[source,kotlin]
import android.app.Dialog
import android.os.Bundle
import androidx.appcompat.app.AlertDialog
import androidx.fragment.app.DialogFragment
class AlertDialogModoAviao(private val mensagem: String) : DialogFragment() {
    override fun onCreateDialog(savedInstanceState: Bundle?): Dialog {
        // requireActivity() garante um contexto válido e não nulo do tipo FragmentActivity
        return AlertDialog.Builder(requireActivity())
            .setTitle("Modo Avião")
            .setMessage(mensagem)
            .setPositiveButton("OK", null)
            .create()
    }
}

- MainActivity.kt
[source.kotlin]
import android.content.Intent
import android.content.IntentFilter
import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.seuprojeto.databinding.ActivityMainBinding // Ajuste para o pacote correto do seu app
class MainActivity : AppCompatActivity() {
    private lateinit var binding: ActivityMainBinding
    private lateinit var myBroadcast: MyBroadcast
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivityMainBinding.inflate(layoutInflater)
        setContentView(binding.root)
        binding.textViewStatus.text = "Monitorando o Modo Avião..."
        // 'this' é passado de forma segura pois AppCompatActivity herda de FragmentActivity
        myBroadcast = MyBroadcast(this)
    }
    override fun onStart() {
        super.onStart()
        // Registro em tempo de execução obrigatório para este broadcast nas APIs modernas do Android
        val filter = IntentFilter(Intent.ACTION_AIRPLANE_MODE_CHANGED)
        registerReceiver(myBroadcast, filter)
    }
    override fun onStop() {
        super.onStop()
        // Desregistro obrigatório para prevenir vazamentos de contexto e memória
        unregisterReceiver(myBroadcast)
    }
}
